public class ArrayPrinter {
    // Print only within the bounds of the array
    public void printArray(int[] arr) {
        for (int i = 0; i < arr.length; i++) {
            System.out.println(arr[i]);
        }
    }
}

