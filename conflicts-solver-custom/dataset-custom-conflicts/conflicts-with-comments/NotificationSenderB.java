public class NotificationSender {
    // The message should have a recipient
    public void send(String message, String recipient) {
        System.out.println("Sending message to " + recipient + ": " + message);
    }
}

